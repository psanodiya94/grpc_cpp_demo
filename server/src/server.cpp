#include "server.h"
#include <iostream>

namespace server {

std::string Server::SayHello(const std::string &name) {
  // placeholder implementation
  return "Hello, " + name + " from server";
}

} // namespace server
